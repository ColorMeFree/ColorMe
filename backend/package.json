{
  "name": "colorbook-backend",
  "version": "1.0.0",
  "description": "Cloudflare Workers backend for ColorMeFree coloring book service",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.3.0",
    "wrangler": "^4.33.1"
  }
}
